size(100, 100);

int count = 0;
int grid = 10;

for (int x = 0; x < width; x+=grid) {
  for (int y = 0; y < height; y+=grid) {
    
    for (int i = x; i < x+grid; i++) {
      for (int j = y; j < y+grid; j++) {
        
      }
    }
    
  }
}

println(count);
